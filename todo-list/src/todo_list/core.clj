(ns todo-list.core
  (:gen-class))

(defn hello [] 
	(println "Hello world!"))

(defn -main [] 
	(hello))

; (defn -main
;   "I don't do a whole lot ... yet."
;   [& args]
;   (println "Hello, World!"))
